apiVersion: v1
kind: Service
metadata:
  name: angular-frontend
  namespace: apps
  labels:
    app: angular-frontend
spec:
  selector:
    app: angular-frontend
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
