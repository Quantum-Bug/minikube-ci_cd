apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-backend
  namespace: apps
  labels:
    app: python-backend
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-backend
  template:
    metadata:
      labels:
        app: python-backend
        tier: backend
    spec:
      containers:
        - name: python-backend
          image: python-backend:ec3cbff-1786238914
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
